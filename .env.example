# PostgreSQL values are injected automatically by Cafe24 AI SPACE.
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
# Set a long random value in production.
SECRET_KEY=
# Used only once when the users table is empty. Mark the password as secret.
BOOTSTRAP_ADMIN_ID=
BOOTSTRAP_ADMIN_PASSWORD=
BOOTSTRAP_ADMIN_NAME=

